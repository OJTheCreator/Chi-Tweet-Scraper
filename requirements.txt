# Chi Tweet Scraper v1.4.0 - Requirements
# =========================================
# Install with: pip install -r requirements.txt

# Core scraping library (Twitter/X API wrapper)
twikit>=1.0.0

# HTTP requests for API calls
requests>=2.28.0

# Excel file handling
openpyxl>=3.0.0

# Data processing
pandas>=1.5.0

# Image handling for GUI
Pillow>=9.0.0

# Async HTTP (used by twikit)
httpx>=0.24.0

# =========================================
# Optional - for additional features:
# =========================================

# PDF documentation generation
reportlab>=4.0.0

# Parquet export (data science)
# pyarrow>=12.0.0

# Google Sheets integration
# google-auth-oauthlib>=1.0.0
# google-api-python-client>=2.0.0

# =========================================
# Standard library modules used (no install needed):
# - tkinter (GUI)
# - asyncio (async operations)
# - threading (background tasks)
# - json (data storage)
# - os, sys (system operations)
# - datetime (date handling)
# - dataclasses (data structures)
# - typing (type hints)
# - collections (Counter for analytics)
# - pathlib (path handling)
# - logging (debug logs)
# - webbrowser (opening URLs)
# - subprocess (cross-platform folder open)
# - sqlite3 (SQLite export)
# =========================================

